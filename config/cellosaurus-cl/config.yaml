link:
  forward: TIO_000148
  reverse: TIO_000149
  file: sample.tsv
update:
  frequency: Quarterly
  method: awk 'BEGIN{RS="\n//\n"; FS="\n"}{n=0;for(i=1; i<=NF; i++){if($i ~ /^AC/){ac=substr($i, 11)}if($i ~ /^CC   Cell type:.*; CL=CL_/){line=$i;sub(/^.*CL=CL_/, "", line);sub(/\.$/,"",line);n++; cl[n]=line}} for(i=1; i<=n; i++){print ac "\t" cl[i]} delete cl}' $TOGOID_ROOT/input/cellosaurus/cellosaurus.txt
