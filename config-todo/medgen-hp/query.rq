# endpoint https://togovar-dev.biosciencedbc.jp/sparql
PREFIX mo: <http://med2rdf/ontology/medgen#>
PREFIX dct: <http://purl.org/dc/terms/>
SELECT DISTINCT ?medgen_id ?hp_id
WHERE {
  ?medgen_uri dct:identifier ?medgen_id ;
              mo:mgconso ?mgconso .
  ?mgconso dct:source mo:HPO ;
           rdfs:seeAlso ?hp .
  BIND (replace(str(?hp), 'http://purl.obolibrary.org/obo/HP_', '') AS ?hp_id)
}
